extends layout

block content
    h1 📚 #{title}

    ul
        each book in book_list
            li
                a(href=`/book/${book._id}`) #{book.title} 
                | - #{book.author} 
        else
            li There are no books.